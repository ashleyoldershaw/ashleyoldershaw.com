{
  "private": true,
  "dependencies": {
    "@sanity/client": "^3.2.0"
  },
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write",
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,json}": "npm run format"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.4",
    "prettier": "^2.5.1"
  }
}
