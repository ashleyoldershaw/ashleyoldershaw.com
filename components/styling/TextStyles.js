import styled from "styled-components";

const fontFamily = `'Caveat', serif`;

export const PageTitle = styled.h1`
  font-family: ${fontFamily};
  font-size: 64px;
  font-weight: 10;
`;

export const PageSubtitle = styled.h2`
  font-family: ${fontFamily};
  font-size: 32px;
  font-weight: 10;
`;
